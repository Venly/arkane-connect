export enum BuildTransactionRequestType {
    TRANSFER = 'TRANSFER',
    GAS_TRANSFER = 'GAS_TRANSFER',
    TOKEN_TRANSFER = 'TOKEN_TRANSFER',
    NFT_TRANSFER = 'NFT_TRANSFER',
    CONTRACT_EXECUTION = 'CONTRACT_EXECUTION',
}
